{
  "name": "server",
  "version": "1.0.0",
  "description": "1. Users can create events\r 2. Users can join events\r 3. Users can view events and participants\r 4. Hosts can remove users\r 5. Hosts can update event details\r 6. Users can leave events\r 7. Hosts can delete events",
  "main": "server.js",
  "scripts": {
    "test": "cross-env RUN_MODE=test jest",
    "cypress": "cross-env RUN_MODE=cypress node server.js",
    "start": "node server.js",
    "pm2": "pm2 start server.js --watch",
    "pm2-stop": "pm2 stop server.js",
    "pm2-log": "pm2 logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "fs": "^0.0.1-security",
    "mysql2": "^3.15.1",
    "path": "^0.12.7",
    "pm2": "^6.0.13"
  },
  "devDependencies": {
    "better-sqlite3": "^12.4.1",
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}
